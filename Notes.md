# Install Kerberos Client Tools
sudo apt-get install -y krb5-user libkrb5-dev python3-dev

# Install Python Kerberos Library
sudo apt install python3-pip
sudo apt install python3-requests-kerberos

# Set Display Resolution (Windows)
Set-VMVideo -VMName "Ubunt01" -HorizonalResolution 1920 -VerticalResolution 1080