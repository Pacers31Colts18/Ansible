---
all:
  children:
    windows:
      hosts:
        dc01:
          ansible_host: dc01.joeloveless.com

      vars:
        #WinRM over HTTPS
        ansible_connection: winrm
        ansible_port: 5986
        ansible_winrm_scheme: https
        ansible_winrm_transport: kerberos

        #Ignore Cert, no CA server
        ansible_winrm_server_cert_validation: ignore

    domain_controllers:
      hosts:
        dc01:
